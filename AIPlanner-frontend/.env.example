# Dev (with Vite proxy): leave empty so requests go to /api on the same origin.
# Dev (direct) or production: set your Spring Boot base URL.
VITE_BACKEND_URL=
# VITE_BACKEND_URL=http://localhost:8080
